#include <stdio.h>

// Operadores de inclemento e degremento
void main() {
    int n = 8;
    printf("O valor e %i", n++);
    printf("\nE no final vale %i", n);
    /*int x = 5;
    int y = 3 + ++x;
    printf("Os valores sao %i e %i", x, y);*/
}
/*
x = x + 1 x++ so aumenta um com mais numeros é usando o x += 1
y = y - 1 y-- so tira um com mais numeros é usando o y -= 1
++n soma antes de fazer qualquer coisa da linha onde esta localizada, n++ so soma depois que sair da linha.
*/