#include <stdio.h>
void main() {
        /*
        //Soma
        float a = 3;
        int b = 2;
        int r = a + b;   // -b, +b operador unario| a + b binario
        printf("O resoltado de a + b e %i", r);*/
        /*
        //Divisao
        float a = 3; // um float e um int para fazer divisao real e dois int pra divisao inteira
        int b = 2;
        float r = a / b;
        printf("O resultado e %.2f", r);*/
        /*
        float n1 = 5.5;
        float n2 = 7;
        float m = (n1 + n2) / 2;
        printf("%f", m );*/
        /*
        //desconto 
        float preco = 123.45;
        float desc = preco * 10 / 100; // (valor orginal|porcentagem que deseja)
        printf("%.2f", desc);*/
    char l = 'J';
    l = l + 1;  // 1 quantidades de letra depois da letra j, pode ser qualquer outra. + pra poxima letra e - pra anterior
    printf("Estou com a letra %i na memoria", l);  //Ele mostra o numero da letra, se por o desse jeito.      
}

/*
precedencia
()
+- unarios
*%/ mesma ordem
+- multiplica�ao e subtra�ao*/
