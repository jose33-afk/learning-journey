#include <stdio.h>
void main() {
    /*
    && == E em python
    || == OU em Python
    ! == NAO em Python
    */
    int a = 5, b = 4, c = 2;
    printf("%s", a>b || a>c && !c!=4?"Verdadeiro":"Falso");
    // v 
}